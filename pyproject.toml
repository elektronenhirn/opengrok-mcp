[project]
name = "opengrok-mcp"
version = "0.1.0"
description = "MCP bindings for OpenGrok"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.25.0",
    "requests>=2.32.5",
]
